openapi: 3.1.0
info:
  title: raphael-graph
  version: 0.1.0
  description: Knowledge graph — relationships, dependencies, visualization
paths:
  /health:
    get:
      summary: Health check
      responses:
        '200':
          description: OK
